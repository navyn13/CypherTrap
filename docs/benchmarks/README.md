# Benchmarks

Latency and throughput measurements for CypherTrap internals.

| Doc | What it covers |
|-----|----------------|
| [Mac M1 — Redis Check chunking](./mac-m1-redis-chunking.md) | Per-call `Check` latency **without** vs **with** Redis chunking |

Hardware for these runs: **Apple MacBook Air (M1), 8 GB, macOS 14.6.1**.
